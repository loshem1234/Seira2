# Hermes agent core dependencies — mirrors pyproject.toml's `dependencies`
# list exactly (kept in sync manually; see docs/seira/WIRING.md Part 8).
#
# This entire set was installed and verified in isolation (fresh venv,
# plain pip, python:3.12-slim-compatible, no compiler toolchain) before
# being added here — see DECISIONS.md D133. Notably nemo-relay, which
# was suspected to need special access, installs cleanly from public PyPI.
openai==2.24.0
certifi==2026.5.20
python-dotenv==1.2.2
fire==0.7.1
httpx[socks]==0.28.1
rich==14.3.3
tenacity==9.1.4
pyyaml==6.0.3
ruamel.yaml==0.18.17
requests==2.33.0
jinja2==3.1.6
pydantic==2.13.4
prompt_toolkit==3.0.52
croniter==6.0.0
packaging==26.0
Markdown==3.10.2
PyJWT[crypto]==2.13.0
urllib3>=2.7.0,<3
cryptography==48.0.1
psutil==7.2.2
websockets==15.0.1
pathspec==1.1.1
fastapi>=0.104.0,<1
uvicorn[standard]>=0.24.0,<1
python-multipart>=0.0.9,<1
ptyprocess>=0.7.0,<1; sys_platform != 'win32'
Pillow==12.3.0
nemo-relay>=0.6.0,<0.7; (sys_platform == 'darwin' and platform_machine == 'arm64') or (sys_platform == 'linux' and platform_machine == 'x86_64') or (sys_platform == 'linux' and platform_machine == 'aarch64')
